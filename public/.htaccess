RewriteEngine On
RewriteBase /

# Forçar HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Bloquear acesso a arquivos sensíveis
<FilesMatch "\.(env|git|tsx|ts|jsx|js\.map)$">
  Order Allow,Deny
  Deny from all
</FilesMatch>

# Bloquear pastas sensíveis
RewriteRule ^src/ - [F,L]
RewriteRule ^\.git/ - [F,L]

# Servir ads.txt, robots.txt e sitemap.xml diretamente
RewriteRule ^ads\.txt$ - [L,T=text/plain]
RewriteRule ^robots\.txt$ - [L,T=text/plain]
RewriteRule ^sitemap\.xml$ - [L,T=application/xml]

# Servir arquivos estáticos diretamente (CSS, JS, imagens, etc.)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Padronizar URLs com barra final (exceto arquivos reais)
# Se a URL não tem barra final e não é um arquivo, redireciona para URL com barra
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_URI} !\.\w{2,4}$
RewriteCond %{REQUEST_URI} !/$
RewriteRule ^(.*)$ https://helplistas.com.br/$1/ [R=301,L]

# Servir diretórios com index.html
RewriteCond %{REQUEST_FILENAME} -d
RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^(.*)/?$ $1/index.html [L]

# IMPORTANTE: NÃO HÁ FALLBACK PARA SPA
# URLs inexistentes retornam 404 (comportamento correto para SEO)
# Removido: RewriteRule ^ /index.html [L]

# Forçar Content-Type correto para arquivos específicos
<FilesMatch "^ads\.txt$">
  ForceType text/plain
  Header set Content-Type "text/plain; charset=utf-8"
</FilesMatch>

<FilesMatch "^sitemap\.xml$">
  ForceType application/xml
  Header set Content-Type "application/xml; charset=utf-8"
</FilesMatch>

<FilesMatch "^robots\.txt$">
  ForceType text/plain
  Header set Content-Type "text/plain; charset=utf-8"
</FilesMatch>

# Compressão Gzip
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Cache Headers
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType application/pdf "access plus 1 month"
  ExpiresByType image/x-icon "access plus 1 year"
  ExpiresByType text/plain "access plus 1 hour"
  ExpiresByType text/html "access plus 1 hour"
</IfModule>

# Security Headers
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set X-XSS-Protection "1; mode=block"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Página de erro 404 customizada (opcional)
ErrorDocument 404 /404.html
